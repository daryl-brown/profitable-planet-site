import type { Config } from 'tailwindcss'

// Tailwind v4: most configuration is CSS-first via @theme in globals.css.
// This file is kept minimal. Content detection is automatic in v4.
const config: Config = {}

export default config
